#include "Database.h"

std::string decryptPassword(std::string &pasword) {
  std::string decryptedPassword;

  return decryptedPassword;
}

std::string encryptPassword(std::string &pasword) {
  std::string encryptedPassword;

  return encryptedPassword;
}

